version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=3001

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
